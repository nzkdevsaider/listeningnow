const index = () => {
    return (
        <div className="flex flex-col">
            <p>Normalmente esta no es la forma correcta...</p>
            <p>Intenta completando la dirección URL de esta manera:</p>
            <p className="bg-slate-900 font-mono">/embed/[nombre de usuario...]</p>
        </div>
    );
}

export default index;